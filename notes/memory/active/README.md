# Active Memory

Use this folder for project memory that is currently true and still affects real work.

Examples:

- current operating rules
- active migration notes
- temporary but still-live constraints

Keep entries concise.
Rewrite instead of duplicating when the same topic already exists.
