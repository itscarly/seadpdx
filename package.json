{
  "name": "seattle-portland-travel-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Static Seattle and Portland travel itinerary dashboard with synced trip planning and executive spend summaries.",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "validate": "node --check data/trip-data.js && node --check dashboards/js/app.js && node scripts/audit-budget.js && node scripts/sync-calendar-exports.js",
    "audit:notes": "node scripts/audit-notes.js",
    "audit:budget": "node scripts/audit-budget.js",
    "update:verification": "node scripts/update-verification-summary.js",
    "sync:calendar": "node scripts/sync-calendar-exports.js",
    "session-status": "node scripts/session-status.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
